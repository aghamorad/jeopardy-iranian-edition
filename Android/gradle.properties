org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

# Pinned, because the default JDK on this machine is 26 and the Android Gradle
# Plugin refuses anything past 21. Temurin 17 is already installed alongside it
# and is the version AGP 8.5 is built and tested against.
org.gradle.java.home=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home
